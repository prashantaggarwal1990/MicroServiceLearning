If S.C("MASK_SHIPPER...") Exists && Is Populated:

 If SafeBool(S.C("MASK_SHIPPER...") = true THEN send Yes.

ElseIf S.C("MASK_SHIPPER...").ToUpper() = "FALSE", "F", "0", or "N", THEN send No.

Else - Defer to Shipper Config Default.

Else - Defer to Shipper Config Default